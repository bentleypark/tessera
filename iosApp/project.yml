name: iosApp
options:
  bundleIdPrefix: com.github.bentleypark.tessera
  deploymentTarget:
    iOS: "15.0"

settings:
  base:
    ENABLE_USER_SCRIPT_SANDBOXING: "NO"

targets:
  iosApp:
    type: application
    platform: iOS
    sources:
      - iosApp
    info:
      path: iosApp/Info.plist
      properties:
        CFBundleDisplayName: "Tessera Sample"
        CADisableMinimumFrameDurationOnPhone: true
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bentley.tessera.sample
        INFOPLIST_FILE: iosApp/Info.plist
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: Z2392B3AD7
        FRAMEWORK_SEARCH_PATHS:
          - "$(SRCROOT)/../tessera-coil/build/bin/currentFramework"
        OTHER_LINKER_FLAGS: "-ObjC"
        ENABLE_BITCODE: "NO"
        STRIP_BITCODE_FROM_COPIED_FILES: "NO"
    dependencies:
      - framework: "../tessera-coil/build/bin/currentFramework/TesseraCoil.framework"
        embed: true
    preBuildScripts:
      - name: "Build KMP Framework"
        script: |
          cd "$SRCROOT/.."
          COIL_DIR="tessera-coil/build/bin/currentFramework"
          rm -f "$COIL_DIR"
          if [ "${PLATFORM_NAME}" = "iphonesimulator" ]; then
            ./gradlew :tessera-coil:linkDebugFrameworkIosSimulatorArm64
            ln -sf "$(pwd)/tessera-coil/build/bin/iosSimulatorArm64/debugFramework" "$COIL_DIR"
          else
            ./gradlew :tessera-coil:linkDebugFrameworkIosArm64
            ln -sf "$(pwd)/tessera-coil/build/bin/iosArm64/debugFramework" "$COIL_DIR"
          fi
        basedOnDependencyAnalysis: false
