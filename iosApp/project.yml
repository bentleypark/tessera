name: iosApp
options:
  bundleIdPrefix: com.naemomlab.tessera
  deploymentTarget:
    iOS: "15.0"

settings:
  base:
    ENABLE_USER_SCRIPT_SANDBOXING: "NO"

targets:
  iosApp:
    type: application
    platform: iOS
    sources:
      - iosApp
    info:
      path: iosApp/Info.plist
      properties:
        CADisableMinimumFrameDurationOnPhone: true
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bentley.tessera.sample
        INFOPLIST_FILE: iosApp/Info.plist
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: Z2392B3AD7
        FRAMEWORK_SEARCH_PATHS:
          - "$(SRCROOT)/../tessera-core/build/bin/currentFramework"
        OTHER_LINKER_FLAGS: "-ObjC"
        ENABLE_BITCODE: "NO"
        STRIP_BITCODE_FROM_COPIED_FILES: "NO"
    dependencies:
      - framework: "../tessera-core/build/bin/currentFramework/TesseraCore.framework"
        embed: true
    preBuildScripts:
      - name: "Build KMP Framework"
        script: |
          cd "$SRCROOT/.."
          FRAMEWORK_DIR="tessera-core/build/bin/currentFramework"
          rm -f "$FRAMEWORK_DIR"
          if [ "${PLATFORM_NAME}" = "iphonesimulator" ]; then
            ./gradlew :tessera-core:linkDebugFrameworkIosSimulatorArm64
            ln -sf "$(pwd)/tessera-core/build/bin/iosSimulatorArm64/debugFramework" "$FRAMEWORK_DIR"
          else
            ./gradlew :tessera-core:linkDebugFrameworkIosArm64
            ln -sf "$(pwd)/tessera-core/build/bin/iosArm64/debugFramework" "$FRAMEWORK_DIR"
          fi
        basedOnDependencyAnalysis: false
